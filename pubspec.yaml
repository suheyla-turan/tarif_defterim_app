name: tarif_defterim
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Temel paketler
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  smooth_page_indicator: ^1.1.0
  shared_preferences: ^2.2.3

  # 🔹 State management & routing (Riverpod istersen kullan)
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0

  # 🔹 Firebase
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  cloud_functions: ^5.1.3
  image_picker: ^1.1.2

  # 🔹 Sesli asistan / TTS
  speech_to_text: ^7.3.0
  flutter_tts: ^4.0.2

  # 🔹 Yardımcılar
  intl: ^0.19.0
  uuid: ^4.5.1
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/chef_ai.png

flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path_android: "assets/images/chef_ai.png"
  image_path_ios: "assets/images/chef_ai.png"
  adaptive_icon_background: "#00000000"   # tamamen şeffaf
  adaptive_icon_foreground: "assets/images/chef_ai.png"

