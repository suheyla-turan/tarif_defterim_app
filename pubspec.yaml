name: tarif_defterim
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔹 State management ve yönlendirme
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0

  # 🔹 Firebase
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  cloud_functions: ^5.1.3

  # 🔹 Sesli asistan (Çırak)
  speech_to_text: ^6.6.2
  flutter_tts: ^4.0.2

  # 🔹 Yardımcılar
  intl: ^0.19.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

